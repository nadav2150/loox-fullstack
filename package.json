{
  "name": "loox",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "appStart": "concurrently \"cd server && npm run start\" \"cd client && npm run dev\"",
    "installAll":"concurrently \"cd server && npm install\" \"cd client && npm install\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
